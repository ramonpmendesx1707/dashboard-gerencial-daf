import {createRequire} from 'node:module';import {dirname} from 'node:path';import {spawnSync} from 'node:child_process';const require=createRequire(import.meta.url);const loader=require.resolve('tsx',{paths:[dirname(require.resolve('vite/package.json'))]});const result=spawnSync(process.execPath,['--import',loader,'tests/operational.test.mjs'],{stdio:'inherit'});process.exit(result.status??1);
